@page "{id:long}"
@model Board_Game_Software.Pages.Match.ResultsModel
@using Board_Game_Software.Models
@{
    ViewData["Title"] = "Record Results";
    var isLocked = Model.MatchComplete == true;
}

<style>
    .game-banner-header {
        height: 220px;
        position: relative;
        overflow: hidden;
        border-radius: 20px;
        background: #1a1d20;
    }

    .banner-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        filter: brightness(0.4) blur(1px);
    }

    .banner-content {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        display: flex;
        flex-direction: column;
        justify-content: center;
        padding: 2.5rem;
    }

    .shelf-flash-card {
        background: linear-gradient(135deg, #16222a, #3a6073);
        border: 2px solid #0dcaf0;
        border-radius: 15px;
        padding: 1.5rem;
        box-shadow: 0 10px 30px rgba(0,0,0,0.5);
    }

    .shelf-location-text {
        font-size: 2.25rem;
        font-weight: 900;
        color: #0dcaf0;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .player-card {
        background: var(--bs-body-tertiary);
        border: 1px solid var(--bs-border-color);
        border-radius: 15px;
        transition: all 0.2s;
        position: relative;
    }

        .player-card:hover {
            border-color: var(--bs-primary);
        }

    .player-avatar-circle {
        width: 50px;
        height: 50px;
        object-fit: cover;
        border-radius: 50%;
        border: 2px solid var(--bs-primary);
    }

    .marker-overlay {
        width: 24px;
        height: 24px;
        position: absolute;
        bottom: -2px;
        right: -2px;
        background: #1a1d20;
        border: 1px solid #3d4246;
        border-radius: 4px;
        padding: 2px;
        object-fit: contain;
    }

    .score-input {
        font-size: 1.5rem;
        font-weight: 800;
        text-align: center;
        color: var(--bs-primary);
    }

    .winner-checkbox:checked + .winner-btn {
        background: #ffc107;
        border-color: #ffc107;
        color: #000;
        box-shadow: 0 0 15px rgba(255,193,7,0.4);
    }

    .winner-btn {
        border-radius: 50px;
        font-weight: bold;
        transition: all 0.3s;
    }

    .player-card.border-warning {
        border-color: #ffc107 !important;
        border-width: 2px;
    }

    .locked-badge {
        position: absolute;
        top: 15px;
        right: 20px;
        background: #dc3545;
        color: white;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: bold;
        z-index: 5;
    }

    .banner-title-link {
        text-decoration: none;
        color: white;
        display: inline-block;
        transition: opacity 0.2s;
    }

        .banner-title-link:hover {
            opacity: 0.8;
            color: #0dcaf0;
        }

    .rating-impact-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
        gap: .75rem;
    }

    .rating-impact-card {
        border: 1px solid var(--bs-border-color);
        border-radius: 8px;
        background: var(--bs-body-tertiary);
        padding: .85rem;
    }

    .stepper-wrap {
        display: grid;
        grid-template-columns: 42px minmax(70px, 1fr) 42px;
        gap: .35rem;
        align-items: center;
    }

    @@media (max-width: 767.98px) {
        .game-banner-header { height: auto; min-height: 210px; border-radius: 8px; }
        .banner-content { padding: 1.25rem; }
        .player-card { border-radius: 8px; }
        .player-card .row { row-gap: 1rem; }
        .player-avatar-circle { width: 64px; height: 64px; }
        .score-input { min-height: 48px; font-size: 1.35rem; }
        .winner-btn, .quick-winner-btn { width: 100%; min-height: 46px; }
        .results-actions { position: sticky; bottom: .75rem; z-index: 20; background: rgba(22,25,28,.94); border: 1px solid var(--bs-border-color); border-radius: 8px; padding: .75rem; }
        .results-actions .btn { width: 100%; }
    }
</style>

<div class="container py-4" style="max-width: 960px;">

    @* FLASH PROMPT: Shows only once after Completion *@
    @if (TempData["FlashShelfLocation"] != null)
    {
        <div class="shelf-flash-card mb-5 text-center">
            <div class="d-flex align-items-center justify-content-center gap-3 mb-2">
                <i class="bi bi-info-circle-fill text-info fs-4"></i>
                <h5 class="text-white fw-bold mb-0">Match Recorded. Return game to:</h5>
            </div>
            <div class="shelf-location-text">
                <i class="bi bi-bookshelf me-2"></i> @TempData["FlashShelfLocation"]
            </div>
            <p class="text-white-50 small mb-0 mt-2">This prompt will disappear when you leave the page.</p>
        </div>
    }

    @if (isLocked)
    {
        <div class="alert alert-danger rounded-4 shadow-sm mb-4 d-flex align-items-center justify-content-between">
            <div class="d-flex align-items-center">
                <i class="bi bi-lock-fill fs-4 me-3"></i>
                <div>
                    <strong>Match Locked!</strong> This match has been completed and editing is disabled.
                </div>
            </div>
            @if (User.IsInRole("Admin"))
            {
                <form method="post" asp-page-handler="Unlock">
                    <input type="hidden" asp-for="Input.MatchId" />
                    <button type="submit" class="btn btn-sm btn-outline-danger rounded-pill px-3">
                        <i class="bi bi-unlock-fill me-1"></i> Unlock Match
                    </button>
                </form>
            }
        </div>
    }

    <form method="post">
        <input type="hidden" asp-for="Input.MatchId" />
        <input type="hidden" asp-for="Input.NightId" />

        <div class="game-banner-header mb-5 shadow-lg">
            <img src="@(Model.GameBannerUrl ?? "/images/default-banner.png")" class="banner-img" />
            <div class="banner-content">
                @if (isLocked)
                {
                    <div class="locked-badge"><i class="bi bi-lock-fill"></i> LOCKED</div>
                }
                <span class="badge bg-primary align-self-start mb-2 rounded-pill px-3">RECORD RESULTS</span>

                @* Game Title with New Tab Link *@
                <a href="/Browsing/BoardGames/BoardGameDetails/@Model.MatchGameId" target="_blank" class="banner-title-link">
                    <h1 class="display-5 fw-bold mb-1">
                        @Model.MatchGameName <i class="bi bi-box-arrow-up-right fs-5 ms-2 opacity-50"></i>
                    </h1>
                </a>

                <p class="text-white-50 mb-0">@Model.MatchDate?.ToString("dddd, MMMM dd, yyyy")</p>
                <div class="mt-3 bg-dark bg-opacity-50 p-2 rounded-4 d-inline-flex align-items-center" style="max-width: fit-content;">
                    <label class="text-white small fw-bold text-uppercase mx-3 opacity-75">Finish Time</label>
                    <input asp-for="Input.FinishedDate" type="datetime-local" class="form-control form-control-sm border-0 bg-transparent text-white fw-bold" disabled="@(isLocked ? "disabled" : null)" />
                </div>
            </div>
        </div>

        <div class="row g-3">
            @for (var i = 0; i < Model.Input.PlayerResults.Count; i++)
            {
                var pr = Model.Input.PlayerResults[i];
                var player = Model.Players.First(p => p.MatchPlayerId == pr.MatchPlayerId);

                <div class="col-12">
                    <div class="player-card p-3 shadow-sm border-start border-4 @(pr.IsWinner ? "border-warning" : "border-secondary") @(isLocked ? "opacity-75" : "")">
                        <input type="hidden" asp-for="Input.PlayerResults[i].MatchPlayerId" />
                        <div class="row align-items-center">
                            <div class="col-md-4">
                                <div class="d-flex align-items-center gap-3">
                                    <div class="position-relative">
                                        <img src="@(player.PlayerAvatarUrl ?? "/images/default-avatar.png")" class="player-avatar-circle" />
                                        @if (!string.IsNullOrWhiteSpace(player.MarkerImageDataUrl))
                                        {
                                            <img src="@player.MarkerImageDataUrl" class="marker-overlay shadow-sm" />
                                        }
                                    </div>
                                    <div class="overflow-hidden">
                                        <h5 class="fw-bold mb-0 text-truncate">@player.PlayerName</h5>
                                        <span class="text-primary small fw-semibold">@player.MarkerTypeName</span>
                                    </div>
                                </div>
                            </div>

                            <div class="col-md-4">
                                <div class="row g-2">
                                    <div class="@(Model.ShowTeams ? "col-6" : "col-12")">
                                        <label class="extra-small fw-bold opacity-50 text-uppercase mb-1">Result Type</label>
                                        <select asp-for="Input.PlayerResults[i].ResultTypeId"
                                                class="form-select form-select-sm result-type-select"
                                                data-index="@i"
                                                disabled="@(isLocked ? "disabled" : null)">
                                            @foreach (var rt in Model.ResultTypes)
                                            {
                                                @if (!Model.IsTeamVictoryGame || rt.Name.Contains("Team"))
                                                {
                                                    <option value="@rt.Id"
                                                            data-victory="@rt.IsVictory.ToString().ToLower()"
                                                            data-defeat="@rt.IsDefeat.ToString().ToLower()">
                                                        @rt.Name
                                                    </option>
                                                }
                                            }
                                        </select>
                                    </div>
                                    @if (Model.ShowTeams)
                                    {
                                        <div class="col-6">
                                            <label class="extra-small fw-bold opacity-50 text-uppercase mb-1">Team</label>
                                            <select asp-for="Input.PlayerResults[i].FinalTeam" class="form-select form-select-sm"
                                                    asp-items="Html.GetEnumSelectList<FinalTeam>()"
                                                    disabled="@(isLocked ? "disabled" : null)">
                                                <option value="">None</option>
                                            </select>
                                        </div>
                                    }
                                </div>
                            </div>

                            <div class="col-md-2 text-center">
                                @if (Model.ShowPoints)
                                {
                                    <label class="small fw-bold opacity-50 text-uppercase mb-1">Score</label>
                                    <div class="stepper-wrap">
                                        <button type="button" class="btn btn-outline-secondary score-step" data-index="@i" data-step="-1" disabled="@(isLocked ? "disabled" : null)">
                                            <i class="bi bi-dash-lg"></i>
                                        </button>
                                        <input asp-for="Input.PlayerResults[i].Score" class="form-control score-input bg-transparent p-0 score-field" data-index="@i" disabled="@(isLocked ? "disabled" : null)" />
                                        <button type="button" class="btn btn-outline-secondary score-step" data-index="@i" data-step="1" disabled="@(isLocked ? "disabled" : null)">
                                            <i class="bi bi-plus-lg"></i>
                                        </button>
                                    </div>
                                }
                                else
                                {
                                    <label class="small fw-bold opacity-50 text-uppercase mb-1">Place</label>
                                    <div class="stepper-wrap">
                                        <button type="button" class="btn btn-outline-secondary place-step" data-index="@i" data-step="-1" disabled="@(isLocked ? "disabled" : null)">
                                            <i class="bi bi-dash-lg"></i>
                                        </button>
                                        <select asp-for="Input.PlayerResults[i].Score" class="form-select placement-field" data-index="@i" disabled="@(isLocked ? "disabled" : null)">
                                            <option value="">-</option>
                                            @for (var place = 1; place <= Model.Input.PlayerResults.Count; place++)
                                            {
                                                <option value="@place">@place</option>
                                            }
                                        </select>
                                        <button type="button" class="btn btn-outline-secondary place-step" data-index="@i" data-step="1" disabled="@(isLocked ? "disabled" : null)">
                                            <i class="bi bi-plus-lg"></i>
                                        </button>
                                    </div>
                                }
                            </div>

                            <div class="col-md-2 text-end">
                                <input type="checkbox" class="btn-check winner-checkbox" id="win-@i" checked="@pr.IsWinner" disabled />
                                <label class="btn btn-outline-secondary btn-sm winner-btn px-4" for="win-@i">
                                    <i class="bi bi-trophy-fill me-1"></i> Win
                                </label>
                                <button type="button" class="btn btn-outline-warning btn-sm quick-winner-btn mt-2 px-4" data-index="@i" disabled="@(isLocked ? "disabled" : null)">
                                    <i class="bi bi-lightning-charge-fill me-1"></i> Toggle
                                </button>
                                <input type="hidden" asp-for="Input.PlayerResults[i].IsWinner" class="hidden-winner" data-index="@i" />
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>

        <div class="rating-impact-card mt-4">
            <div class="d-flex flex-column flex-md-row justify-content-between gap-2 mb-3">
                <div>
                    <h3 class="h5 fw-bold mb-1"><i class="bi bi-activity text-info me-2"></i>Rating Impact</h3>
                    <div class="text-secondary small">@Model.RatingMethodReason</div>
                </div>
                <span class="badge text-bg-info align-self-start">@Model.RatingMethodName</span>
            </div>
            <div class="rating-impact-grid">
                @foreach (var player in Model.Players)
                {
                    <div class="rating-impact-card">
                        <div class="fw-bold text-truncate">@player.PlayerName</div>
                        <div class="d-flex justify-content-between small text-secondary mt-2">
                            <span>Pre</span>
                            <span>@(player.PreMatchRatingMu?.ToString("0.00") ?? "-")</span>
                        </div>
                        <div class="d-flex justify-content-between small">
                            <span>Delta</span>
                            <span class="@(player.RatingChangeMu >= 0 ? "text-success" : "text-danger")">@(player.RatingChangeMu?.ToString("+0.00;-0.00;0.00") ?? "-")</span>
                        </div>
                        <div class="d-flex justify-content-between small text-secondary">
                            <span>Post</span>
                            <span>@(player.PostMatchRatingMu?.ToString("0.00") ?? "-")</span>
                        </div>
                    </div>
                }
            </div>
        </div>

        <div class="mt-5 text-center d-flex flex-column align-items-center gap-3 results-actions">
            @if (!isLocked)
            {
                <div class="d-flex gap-3">
                    <button type="submit" class="btn btn-primary btn-lg rounded-pill px-5 shadow-lg">Save Draft</button>
                    <button type="submit" asp-page-handler="Complete" class="btn btn-success btn-lg rounded-pill px-5 shadow-lg">
                        <i class="bi bi-check-circle-fill me-2"></i>Complete Match
                    </button>
                </div>
            }
            <div class="mt-2">
                <a asp-page="/GameNight/Details" asp-route-id="@Model.NightId" class="text-secondary text-decoration-none small">
                    <i class="bi bi-arrow-left me-1"></i> Return to Game Night
                </a>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            const isPointGame = @Model.ShowPoints.ToString().ToLower();

            $('.result-type-select').on('change', function() {
                const $select = $(this);
                const index = $select.data('index');
                const $selectedOption = $select.find('option:selected');
                const isVictory = $selectedOption.data('victory') === true;
                const isDefeat = $selectedOption.data('defeat') === true;
                const $checkbox = $(`#win-${index}`);
                const $hiddenWinner = $(`.hidden-winner[data-index="${index}"]`);
                const $card = $select.closest('.player-card');

                if (isVictory) {
                    $checkbox.prop('checked', true);
                    $hiddenWinner.val('true');
                    $card.removeClass('border-secondary').addClass('border-warning');
                } else if (isDefeat) {
                    $checkbox.prop('checked', false);
                    $hiddenWinner.val('false');
                    $card.removeClass('border-warning').addClass('border-secondary');
                }
            });

            $('.quick-winner-btn').on('click', function() {
                const index = $(this).data('index');
                const $checkbox = $(`#win-${index}`);
                const $hiddenWinner = $(`.hidden-winner[data-index="${index}"]`);
                const $card = $(this).closest('.player-card');
                const next = !$checkbox.prop('checked');
                $checkbox.prop('checked', next);
                $hiddenWinner.val(next ? 'true' : 'false');
                $card.toggleClass('border-warning', next);
                $card.toggleClass('border-secondary', !next);
            });

            $('.score-step').on('click', function() {
                const index = $(this).data('index');
                const step = parseFloat($(this).data('step'));
                const $field = $(`.score-field[data-index="${index}"]`);
                const current = parseFloat($field.val());
                $field.val((isNaN(current) ? 0 : current) + step).trigger('input');
            });

            $('.place-step').on('click', function() {
                const index = $(this).data('index');
                const step = parseInt($(this).data('step'), 10);
                const $field = $(`.placement-field[data-index="${index}"]`);
                const current = parseInt($field.val(), 10);
                const max = @Model.Input.PlayerResults.Count;
                const next = Math.min(max, Math.max(1, (isNaN(current) ? 1 : current) + step));
                $field.val(next).trigger('change');
            });

            if (isPointGame) {
                $('.score-field').on('input', function() {
                    let maxScore = -Infinity;
                    let winnerIndices = [];
                    $('.score-field').each(function() {
                        const val = parseFloat($(this).val());
                        if (!isNaN(val)) {
                            if (val > maxScore) {
                                maxScore = val;
                                winnerIndices = [$(this).data('index')];
                            } else if (val === maxScore) {
                                winnerIndices.push($(this).data('index'));
                            }
                        }
                    });
                    $('.winner-checkbox').prop('checked', false);
                    $('.hidden-winner').val('false');
                    $('.player-card').removeClass('border-warning').addClass('border-secondary');
                    if (winnerIndices.length > 0 && maxScore !== -Infinity) {
                        winnerIndices.forEach(idx => {
                            $(`#win-${idx}`).prop('checked', true);
                            $(`.hidden-winner[data-index="${idx}"]`).val('true');
                            $(`#win-${idx}`).closest('.player-card').removeClass('border-secondary').addClass('border-warning');
                        });
                    }
                });
            } else {
                $('.placement-field').on('change', function() {
                    const index = $(this).data('index');
                    const place = parseInt($(this).val(), 10);
                    const isWinner = place === 1;
                    const $checkbox = $(`#win-${index}`);
                    const $hiddenWinner = $(`.hidden-winner[data-index="${index}"]`);
                    const $card = $(this).closest('.player-card');

                    $checkbox.prop('checked', isWinner);
                    $hiddenWinner.val(isWinner ? 'true' : 'false');
                    $card.toggleClass('border-warning', isWinner);
                    $card.toggleClass('border-secondary', !isWinner);
                });
            }
        });
    </script>
}
